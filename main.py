import megacode

# Execute pure UC file. Note that it was based off of Python. The easiest way to use it is to take a tutorial on basic Python and write the code in a .uc file.
megacode.runMC(True) # True opens the menu, False runs it in UCpython. For this example, we will use the menu.
